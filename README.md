
# triangdist

The `triangdist` package provides a professional implementation of the Triangular Distribution for R. It includes functions for density, cumulative distribution, quantiles, and random number generation.

## Installation

You can install the development version of **triangdist** from [GitHub](https://github.com/) with:

```r
# install.packages("remotes")
remotes::install_github("locorca/triangdist")
